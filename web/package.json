{
  "name": "motor-agentico-observability-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p ${MOTOR_PUERTO:-8796} -H 127.0.0.1",
    "build": "next build",
    "start": "next start -p ${MOTOR_PUERTO:-8796} -H 127.0.0.1",
    "tipos": "tsc --noEmit --incremental false",
    "test": "node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --disable-warning=ExperimentalWarning --test tests/*.test.ts"
  },
  "dependencies": {
    "next": "16.2.12",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^26.2.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=22.15"
  }
}
